<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CLAWNET Access</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Fira+Code:wght@300;400;500&display=swap');
        body { font-family: 'Fira Code', monospace; }
        .terminal-glow { box-shadow: 0 0 30px rgba(34, 197, 94, 0.15); }
    </style>
</head>
<body class="bg-[#09090b] text-zinc-400 min-h-screen flex items-center justify-center">
    <div class="terminal-glow bg-[#0c0c0e] border border-zinc-700 rounded-lg p-8 max-w-md w-full mx-4">
        <h2 class="text-green-500 text-2xl font-bold mb-2 tracking-tighter">🔒 CLAWNET ACCESS</h2>
        <p class="text-zinc-500 text-xs mb-6">Enter access code to continue</p>

        <% if (error) { %>
        <div class="bg-red-900/20 border border-red-800 text-red-400 px-4 py-2 rounded mb-4 text-xs">
            <%= error %>
        </div>
        <% } %>

        <form action="/dashboard" method="GET" class="space-y-4">
            <input type="text"
                   name="access_code"
                   id="access_code"
                   placeholder="Access Code"
                   autocomplete="off"
                   autocapitalize="off"
                   spellcheck="false"
                   autofocus
                   class="w-full bg-black border border-zinc-700 rounded p-3 text-zinc-100 font-mono uppercase tracking-widest focus:border-green-600 outline-none text-center text-lg">

            <button type="submit"
                    class="w-full bg-green-600 py-3 rounded text-black font-bold uppercase hover:bg-green-700 transition-colors">
                Authenticate
            </button>
        </form>

        <div class="mt-6 pt-4 border-t border-zinc-800 text-center">
            <a href="/" class="text-zinc-500 hover:text-zinc-300 text-xs">← Back to Home</a>
        </div>
    </div>

    <script>
        document.getElementById('access_code').focus();
    </script>
</body>
</html>
