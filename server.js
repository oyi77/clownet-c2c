// ClawNet C2C Relay — Bootstrapper
// This file preserves backward compatibility:
//   npm start / node server.js → works as before
//   Tests that spawn 'node server.js' → still work
require('./src/server');
